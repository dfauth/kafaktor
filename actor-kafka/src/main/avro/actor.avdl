@namespace("com.github.dfauth.actor.kafka.avro")
protocol Actor {

  record Address {
    string topic;
    string key;
  }

  record ActorMessage {
    long timestamp;
    map<string> metadata;
    Address recipient;
    union {null, Address} sender = null;
    string payloadSchema;
    bytes payload;
  }

}