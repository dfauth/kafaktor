@namespace("com.github.dfauth.actor.kafka")
protocol Actor {

  record ActorMessage {
    long timestamp;
    map<string> metadata;
    string recipient;
    union {null, string} sender = null;
    string payloadSchema;
    bytes payload;
  }

}